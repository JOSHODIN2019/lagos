// Stage 01
// Step 14
//
// Purpose: entry route — launches straight into the map screen, no landing page.

import { MapScreen } from "@/components/screens/MapScreen";

export default function Home() {
  return <MapScreen />;
}
